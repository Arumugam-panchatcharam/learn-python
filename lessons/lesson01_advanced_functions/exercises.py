"""Exercises for advanced functions. Do not peek at solutions.py."""


# Exercise 1
# Write a function `repeat(callable, times, value)` that calls `callable(value)`
# exactly `times` times and returns the final result.


# Exercise 2
# Write `make_formatter(prefix: str)` that returns a function accepting `message: str`
# and returning `f"{prefix}: {message}"`.


# Exercise 3
# Write `merge_counts(*counters: dict[str, int]) -> dict[str, int]` using **kwargs
# or explicit iteration. Sum values for duplicate keys.
